function coercion() {
  //any non-primitive will have the following toPrimitive methods
  // hint - Number  =  valueOf() then toString()
  // hint - String  =   toString() then valueOf()
}

export default coercion;
